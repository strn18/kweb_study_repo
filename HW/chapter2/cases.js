const permutation = (n, r) => {
  // return n!/(n-r)!r!;
}

const combination = (n, r) => {
  //
}

const multiPermutation = (n, r) => {
  //
}

modules.export = {
  permutation, 
  combination, 
  multiPermutation, 
}